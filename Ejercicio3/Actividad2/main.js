let numeroSolicitado = parseFloat(prompt("Ingrese un numero."));
let iteraciones = 1
while(iteraciones <= numeroSolicitado && iteraciones < 6) {
    alert(`vez numero ${iteraciones}: lado.`)
    iteraciones++
}